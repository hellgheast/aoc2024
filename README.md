# Advent of Code 2024
* Done in CMake and C++ when free time is available

# Build commands
`cmake -S . -B build && cd build && cmake --build .`

# Toolchain
Use xpack 15.2, that is setup inside the folder aoc

# Toolchain for mac

Fix clang issue `export SDKROOT=$(xcrun --sdk macosx --show-sdk-path)`
